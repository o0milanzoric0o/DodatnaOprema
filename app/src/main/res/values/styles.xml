<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="colorControlNormal">@color/white</item>
    </style>


    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@android:color/white</item>
    </style>

    <style name="HomeAsUpIndicator">
        <item name="color">@android:color/white</item>
    </style>

    <style name="CustomTabTextStyle" parent="TextAppearance.Design.Tab">
        <!-- Customize Tab Text Style. -->
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#8AFFFFFF</item>
        <item name="textAllCaps">true</item>
    </style>

    <style name="CustomNavigationDrawerLayout" parent="Widget.Design.NavigationView">
        <!-- Customize List Item theme. -->
        <item name="android:itemTextAppearance">@style/NavigatonListItemTextStyle</item>
        <item name="tabTextAppearance">@style/CustomTabTextStyle</item>
    </style>

    <style name="NavigatonListItemTextStyle" parent="TextAppearance.Design.Tab">
        <!-- Customize Tab Text Style. -->
        <item name="android:textSize">@dimen/list_item_text_size</item>
        <item name="android:textColor">@color/listItemColor</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark"/>


    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">

    </style>
    
    <!-- Activity with no Title -->
    <style name="ThemeOverlay.MyNoTitleActivity" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Dark Buttons -->
    <style name="ThemeOverlay.MyDarkButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/blue_grey_500</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="CustomSpinnerItemStyle" parent="@android:style/Widget.Holo.DropDownItem.Spinner">
        <item name="colorPrimary">@color/base_gray</item>
        <item name="colorPrimaryDark">@color/base_gray</item>
        <item name="android:textColor">@color/secondary_text</item>
        <item name="android:textAllCaps">true</item>
        <item name="colorControlNormal">@color/secondary_text</item>
        <item name="colorControlActivated">@color/accent</item>
        <item name="colorControlHighlight">@color/accent</item>
    </style>


    <style name="OneArticleTextView" parent="@android:style/TextAppearance.Medium">
        <item name="android:paddingBottom">@dimen/one_product_text_padding_top_bottom</item>
        <item name="android:paddingLeft">@dimen/one_product_text_padding_left_right</item>
        <item name="android:paddingRight">@dimen/one_product_text_padding_left_right</item>
        <item name="android:paddingTop">@dimen/one_product_text_padding_top_bottom</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:text">Medium Text</item>
        <item name="android:background">@color/background_white</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>


    <style name="Filter.DIALOG" parent="AppTheme" >
       <item name="android:windowNoTitle">true</item>
        <!-- <item name="android:windowFullscreen">true</item>-->
        <item name="android:windowIsFloating">false</item>
    </style>
</resources>
